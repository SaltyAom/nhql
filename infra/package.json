{
    "name": "infra",
    "scripts": {
        "up": "cd docker && pulumi up -y && cd ../cloud\\ run && pulumi up -y"
    },
    "devDependencies": {
        "@types/node": "^10.0.0"
    },
    "dependencies": {
        "@pulumi/docker": "^2.5.0",
        "@pulumi/gcp": "^4.0.0",
        "@pulumi/pulumi": "^2.0.0"
    }
}
